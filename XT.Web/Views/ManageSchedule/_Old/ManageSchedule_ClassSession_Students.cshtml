@using XT.BusinessService;
@using XT.Web.External;

@{
    //Trang chính cho quản lý lịch học của lớp theo module
    ViewBag.Title = "[Schedule] Quản lý danh sách HV của lớp";
    var name = "ClassSession_Student";
    var title = ViewBag.Title;
    Layout = "~/Views/Layout/LayoutAdmin.cshtml";
}

@section Scripts{
    @Scripts.Url("~/Scripts/Admin/ManageSchedule_ClassSession_Calendar")
}

<section class="content-header">
    <h1>@title
        <br /><small>C1504L - Typography Techniques (1/5/2016 - 27/5/2016) - FC Đậu Ngọc Hà Dương</small>
    </h1>
    <ol class="breadcrumb low">
        <li><a href="@Url.GoAccountAdminFirstPage()"><i class="fa fa-dashboard"></i>Home</a></li>
        <li><a href="@Url.Action("ManageSchedule_ClassSession")">Danh sách lịch học</a></li>
        <li class="active">C1504L - Typography Techniques</li>
    </ol>
    @*<div class="breadcrumb">
        <div style="margin-bottom: 10px; margin-top: 10px;">
            <button class="btn btn-warning" data-toggle="modal" data-target="#add-modal">
                <i class="fa fa-plus fa-fw"></i>&nbsp;Student
            </button>
        </div>
    </div>*@
</section>
<section class="content">
    <div class="row ajax-search-group">
        <div class="col-lg-12">
            <div class="Search_Result">
                <div class="box">
                    <div class="box-header">
                        <h3 class="box-title">Danh sách HV của lớp</h3>
                        <div class="box-tools">
                            <button class="btn btn-warning" data-toggle="modal" data-target="#add-modal">
                                <i class="fa fa-plus fa-fw"></i>&nbsp;Student
                            </button>
                            <a href="@Url.Action("ManageSchedule_ClassSession_Attendance")" 
                                class="btn btn-default bg-maroon" title="Danh sách ngày học">
                                <i class="fa fa-calendar fa-fw"></i>
                            </a>
                            <a href="@Url.Action("ManageSchedule_ClassSession_Exam")" 
                                class="btn btn-success" title="Danh sách thi">
                                <i class="fa fa-bar-chart fa-fw"></i>
                            </a>
                            <a href="@Url.Action("ManageSchedule_ClassSession_StudentAttendance")" 
                                class="btn btn-warning" title="Điểm danh của HV">
                                <i class="fa fa-star fa-fw"></i>
                            </a>
                        </div>
                    </div>
                    @Html.Partial("Search_Result/_partial_Search_" + name, "")
                </div>
            </div>
        </div>
    </div>
</section>